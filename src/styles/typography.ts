import { StyleSheet } from 'react-native';

export const Typography = StyleSheet.create({
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#000',
  },
  subheading: {
    fontSize: 18,
    fontWeight: '600',
    color: '#333',
  },
  body: {
    fontSize: 14,
    fontWeight: 'normal',
    color: '#444',
  },
});